[phases.setup]
nixPkgs = ["python311", "libGL", "libglib", "tesseract", "zbar"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT --reload"

[variables]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
