rules_version = '2';
// Vision Guard — Règles Storage
// Phase test : tout utilisateur authentifié peut lire/écrire.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
